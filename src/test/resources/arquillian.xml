<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<arquillian
  >
  <container qualifier="openejb" default="true">
    <configuration>
      <property name="properties">
        blog = new://Resource?type=DataSource
        blog.JdbcUrl = jdbc:hsqldb:mem:jeblog
        # blog.LogSql = true

        # useless for our tests
        admin.disabled = true

        # predeploy the war, useless for a single test but avoid deployment/redeployment pattern for multiple tests
        openejb.arquillian.predeploy-archives = com.github.rmannibucau.test.blog.rest.util.ShrinkWraps

        # for debug purpose
        logging.level.org.apache.cxf.jaxrs = FINE
      </property>
    </configuration>
  </container>
</arquillian>
